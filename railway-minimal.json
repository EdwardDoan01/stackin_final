{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "cd Stackin && gunicorn Stackin.wsgi:application --bind 0.0.0.0:$PORT"
  }
}
